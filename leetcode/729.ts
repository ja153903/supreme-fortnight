import { TODO } from "@utils/todo";

class MyCalendar {
  constructor() {}

  book(start: number, end: number): boolean {
    throw new TODO(
      "Figure out how to implement a segment tree for this problem",
    );
  }
}

export { MyCalendar };
