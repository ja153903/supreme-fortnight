import { TODO } from "@utils/todo"

function uniquePathsIII(_grid: number[]): number {
	// 1 is the starting square
	// 2 is the ending square
	// 0 is an empty square you can walk over
	// -1 is an obstacle you cannot walk over
	// return the number of paths where you can walk over -1 once
	throw new TODO("implement this later")
}

export { uniquePathsIII }
